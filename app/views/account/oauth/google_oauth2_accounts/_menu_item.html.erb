<% if can? :read, Oauth::GoogleOauth2Account.new(user: current_user) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_user, :oauth_google_oauth2_accounts]),
    label: t('oauth/google_oauth2_accounts.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('oauth/google_oauth2_accounts.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
