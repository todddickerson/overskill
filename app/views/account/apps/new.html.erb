<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
      <h3 class="text-sm font-semibold text-blue-900 mb-2">🚀 OverSkill - AI App Generation</h3>
      <p class="text-sm text-blue-800">
        Generate complete, working web apps from your ideas! Our AI understands what you want to build and creates all the code for you.
      </p>
      <div class="mt-2 text-xs text-blue-700">
        <strong>Supported app types:</strong>
        <span class="inline-block bg-blue-100 px-2 py-1 rounded mr-1">Todo Lists</span>
        <span class="inline-block bg-blue-100 px-2 py-1 rounded mr-1">Games</span>
        <span class="inline-block bg-blue-100 px-2 py-1 rounded mr-1">Landing Pages</span>
        <span class="inline-block bg-blue-100 px-2 py-1 rounded mr-1">Dashboards</span>
        <span class="inline-block bg-blue-100 px-2 py-1 rounded">+ More</span>
      </div>
    </div>
    
    <%= render 'account/shared/box', divider: true do |box| %>
      <% box.t :description, title: '.header' %>
      <% box.body.render 'form', app: @app %>
    <% end %>
  <% end %>
<% end %>
