<% if can? :read, AppGeneration.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :app_generations]),
    label: t('app_generations.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('app_generations.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
