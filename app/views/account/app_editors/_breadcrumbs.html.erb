<% app ||= @app %>
<% team ||= @team || app&.team %>
<%= render 'account/teams/breadcrumbs', team: team %>
<%= render 'account/shared/breadcrumb', label: t('apps.label'), url: [:account, team, :apps] %>
<% if app&.persisted? %>
  <%= render 'account/shared/breadcrumb', label: app.label_string, url: [:account, app] %>
  <%= render 'account/shared/breadcrumb', label: t('.label'), url: [:account, app, :editor] %>
<% end %>
<%= render 'account/shared/breadcrumbs/actions', only_for: 'app_editors' %>