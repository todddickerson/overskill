<% app_audit_logs = app_audit_logs.accessible_by(current_ability) %>
<% app = @app %>
<% context ||= app %>
<% collection ||= :app_audit_logs %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% pagy ||= nil %>
<% pagy, app_audit_logs = pagy(app_audit_logs, page_param: :app_audit_logs_page) unless pagy %>

<%= action_model_select_controller do %>
  <% cable_ready_updates_for context, collection do %>
    <%= render 'account/shared/box', pagy: pagy do |box| %>
      <% box.title t(".contexts.#{context.class.name.underscore}.header") %>
      <% box.description do %>
        <%= t(".contexts.#{context.class.name.underscore}.description#{"_empty" unless app_audit_logs.any?}") %>
        <%= render "shared/limits/index", model: app_audit_logs.model %>
      <% end %>

      <% box.table do %>
        <% if app_audit_logs.any? %>
          <table class="table">
            <thead>
              <tr>
                <%= render "shared/tables/select_all" %>
                <th><%= t('.fields.action_type.heading') %></th>
                <th><%= t('.fields.performed_by.heading') %></th>
                <th><%= t('.fields.target_resource.heading') %></th>
                <th><%= t('.fields.resource_id.heading') %></th>
                <%# 🚅 super scaffolding will insert new field headers above this line. %>
                <th><%= t('.fields.created_at.heading') %></th>
                <th class="text-right"></th>
              </tr>
            </thead>
            <tbody>
              <%= render partial: 'account/app_audit_logs/app_audit_log', collection: app_audit_logs %>
            </tbody>
          </table>
        <% end %>
      <% end %>

      <% box.actions do %>
        <% unless hide_actions %>
          <% if context == app %>
            <% if can? :create, AppAuditLog.new(app: app) %>
              <%= link_to t('.buttons.new'), [:new, :account, app, :app_audit_log], class: "#{first_button_primary(:app_audit_log)} new" %>
            <% end %>
          <% end %>

          <%# 🚅 super scaffolding will insert new targets one parent action model buttons above this line. %>
          <%# 🚅 super scaffolding will insert new bulk action model buttons above this line. %>
          <%= render "shared/bulk_action_select" if app_audit_logs.any? %>

          <% unless hide_back %>
            <%= link_to t('global.buttons.back'), [:account, context], class: "#{first_button_primary(:app_audit_log)} back" %>
          <% end %>
        <% end %>
      <% end %>

      <% box.raw_footer do %>
        <%# 🚅 super scaffolding will insert new action model index views above this line. %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
