<% if can? :read, AppCollaborator.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :app_collaborators]),
    label: t('app_collaborators.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('app_collaborators.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
