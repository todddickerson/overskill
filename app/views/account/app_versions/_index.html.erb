<% app_versions = app_versions.accessible_by(current_ability) %>
<% team = @team %>
<% context ||= team %>
<% collection ||= :app_versions %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% pagy ||= nil %>
<% pagy, app_versions = pagy(app_versions, page_param: :app_versions_page) unless pagy %>

<%= action_model_select_controller do %>
  <% cable_ready_updates_for context, collection do %>
    <%= render 'account/shared/box', pagy: pagy do |box| %>
      <% box.title t(".contexts.#{context.class.name.underscore}.header") %>
      <% box.description do %>
        <%= t(".contexts.#{context.class.name.underscore}.description#{"_empty" unless app_versions.any?}") %>
        <%= render "shared/limits/index", model: app_versions.model %>
      <% end %>

      <% box.table do %>
        <% if app_versions.any? %>
          <table class="table">
            <thead>
              <tr>
                <%= render "shared/tables/select_all" %>
                <th><%= t('.fields.app.heading') %></th>
                <th><%= t('.fields.user.heading') %></th>
                <th><%= t('.fields.commit_sha.heading') %></th>
                <th><%= t('.fields.commit_message.heading') %></th>
                <th><%= t('.fields.version_number.heading') %></th>
                <%# 🚅 super scaffolding will insert new field headers above this line. %>
                <th><%= t('.fields.created_at.heading') %></th>
                <th class="text-right"></th>
              </tr>
            </thead>
            <tbody>
              <%= render partial: 'account/app_versions/app_version', collection: app_versions %>
            </tbody>
          </table>
        <% end %>
      <% end %>

      <% box.actions do %>
        <% unless hide_actions %>
          <% if context == team %>
            <% if can? :create, AppVersion.new(team: team) %>
              <%= link_to t('.buttons.new'), [:new, :account, team, :app_version], class: "#{first_button_primary(:app_version)} new" %>
            <% end %>
          <% end %>

          <%# 🚅 super scaffolding will insert new targets one parent action model buttons above this line. %>
          <%# 🚅 super scaffolding will insert new bulk action model buttons above this line. %>
          <%= render "shared/bulk_action_select" if app_versions.any? %>

          <% unless hide_back %>
            <%= link_to t('global.buttons.back'), [:account, context], class: "#{first_button_primary(:app_version)} back" %>
          <% end %>
        <% end %>
      <% end %>

      <% box.raw_footer do %>
        <%# 🚅 super scaffolding will insert new action model index views above this line. %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
