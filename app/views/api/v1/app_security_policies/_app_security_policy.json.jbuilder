json.extract! app_security_policy,
  :id,
  :app_id,
  :policy_name,
  :policy_type,
  :enabled,
  :configuration,
  :description,
  :last_violation,
  :violation_count,
  # 🚅 super scaffolding will insert new fields above this line.
  :created_at,
  :updated_at

# 🚅 super scaffolding will insert file-related logic above this line.
