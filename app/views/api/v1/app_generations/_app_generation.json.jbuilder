json.extract! app_generation,
  :id,
  :app_id,
  :status,
  :ai_model,
  :prompt,
  :enhanced_prompt,
  :started_at,
  :completed_at,
  :duration_seconds,
  :input_tokens,
  :output_tokens,
  :total_cost,
  :error_message,
  :retry_count,
  # 🚅 super scaffolding will insert new fields above this line.
  :created_at,
  :updated_at

# 🚅 super scaffolding will insert file-related logic above this line.
