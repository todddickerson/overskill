en:
  oauth/github_accounts: &github_accounts
    label: &label Github Accounts
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fab fa-github-s ti ti-github
    buttons: &buttons
      new: Connect Github Account
      another: Connect Another Github Account
      edit: Settings
      update: Save Settings
      destroy: Disconnect
      shorthand:
        edit: Settings
        destroy: Disconnect
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to disconnect the %{github_account_name} Github account from your user account? This will only disconnect it for use as a single-sign on option. It may still be connected as an integration elsewhere.
    fields: &fields
      name:
        name: &name Name
        label: *name
        heading: *name
        api_title: *name
        api_description: *name
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        name: &created_at Connected
        heading: *created_at
        api_title: *created_at
        api_description: *created_at
    index:
      section: '%{users_possessive} Github Accounts'
      contexts:
        user:
          header: Connected Github Accounts
          description: You can sign into your account with any of the following Github Accounts. You can also manage where these accounts are used as integrations.
          description_empty: "You can connect a Github account to your user account for single sign-on below."
          integration_note: "You can also <a href=\"%{integration_url}\">install Github as an integration</a>."
      fields: *fields
      buttons: *buttons
    show:
      section: "%{github_account_name}"
      header: Connected Github Account
      description: "Your %{github_account_name} Github account is currently connected for single sign-on authentication."
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    edit:
      section: "%{github_account_name}"
      header: Github Account Settings
      description: You may not need this page if your Github integration doesn't have any configurable settings, but we leave the page here by default in case you need it.
      form: *form
    notifications:
      created: Great! We've added that Github account to your account!
      updated: Github account settings were successfully updated.
      destroyed: We've disconnected that Github account from your account.
  account:
    oauth:
      github_accounts: *github_accounts
  activerecord:
    attributes:
      oauth/github_account:
        name: *name
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
