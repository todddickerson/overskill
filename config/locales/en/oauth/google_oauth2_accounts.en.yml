en:
  oauth/google_oauth2_accounts: &google_oauth2_accounts
    label: &label Google Oauth2 Accounts
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fab fa-google_oauth2-s ti ti-google
    buttons: &buttons
      new: Connect Google Oauth2 Account
      another: Connect Another Google Oauth2 Account
      edit: Settings
      update: Save Settings
      destroy: Disconnect
      shorthand:
        edit: Settings
        destroy: Disconnect
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to disconnect the %{google_oauth2_account_name} Google Oauth2 account from your user account? This will only disconnect it for use as a single-sign on option. It may still be connected as an integration elsewhere.
    fields: &fields
      name:
        name: &name Name
        label: *name
        heading: *name
        api_title: *name
        api_description: *name
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        name: &created_at Connected
        heading: *created_at
        api_title: *created_at
        api_description: *created_at
    index:
      section: '%{users_possessive} Google Oauth2 Accounts'
      contexts:
        user:
          header: Connected Google Oauth2 Accounts
          description: You can sign into your account with any of the following Google Oauth2 Accounts. You can also manage where these accounts are used as integrations.
          description_empty: "You can connect a Google Oauth2 account to your user account for single sign-on below."
          integration_note: "You can also <a href=\"%{integration_url}\">install GoogleOauth2 as an integration</a>."
      fields: *fields
      buttons: *buttons
    show:
      section: "%{google_oauth2_account_name}"
      header: Connected Google Oauth2 Account
      description: "Your %{google_oauth2_account_name} Google Oauth2 account is currently connected for single sign-on authentication."
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    edit:
      section: "%{google_oauth2_account_name}"
      header: Google Oauth2 Account Settings
      description: You may not need this page if your GoogleOauth2 integration doesn't have any configurable settings, but we leave the page here by default in case you need it.
      form: *form
    notifications:
      created: Great! We've added that Google Oauth2 account to your account!
      updated: Google Oauth2 account settings were successfully updated.
      destroyed: We've disconnected that Google Oauth2 account from your account.
  account:
    oauth:
      google_oauth2_accounts: *google_oauth2_accounts
  activerecord:
    attributes:
      oauth/google_oauth2_account:
        name: *name
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
